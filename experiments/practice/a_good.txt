동기 Client는 비동기 AsyncClient를 감싸는 래퍼입니다.
client.py의 Client가 내부에 AsyncClient 인스턴스를 들고 있고,
동기 메서드가 호출되면 내부적으로 비동기 메서드를 실행해 결과를 돌려주는 구조입니다.
실제 통신 로직은 async_client.py 한쪽에만 있고 Client에는 중복 구현이 없습니다.

왜 비동기를 먼저 만들고 동기를 감쌌는지는 코드에 적혀 있지 않습니다.
비동기가 먼저였고 동기 사용자를 나중에 지원한 것으로 보이지만,
그 순서를 코드로 확인할 방법은 없어 추측입니다.