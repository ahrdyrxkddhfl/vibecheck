"""본 측정 채점 결과를 exp_a_results.md에 채워 넣는다.

답변 원문이 채점의 근거이므로 파일을 다시 쓰지 않고 채점 칸만 교체한다.
"""

import re
import sys
from pathlib import Path

TARGET = Path("experiments/exp_a_results.md")

EMPTY_BLOCK = re.compile(r"- 점수\(0~2\): *\n- 플래그\(H/A/-\): *\n- 메모: *\n")

GRADES = {
    "Q01": ("2", "-", "정답 청크 miss(client.py::Client). 그러나 _build_parser와 "
                      "ProfileResult/DocumentResult로 프로젝트 성격을 정확히 재구성. "
                      "검색 실패가 답변 실패로 직결되지 않는 사례."),
    "Q02": ("1", "-", "정답 청크 1등. 호출 흐름은 정확하나 SSE/JSON-RPC를 말하지 못함. "
                      "답이 call_tool 본문에 있는데 그 청크가 top_k에 없어 "
                      "'구체적 통신 방식은 알 수 없다'고 기권. 청크 경계 문제."),
    "Q03": ("2", "-", "정답 청크 5등이지만 _parse_response가 1등으로 들어와 보완. "
                      "2단 구조와 예외 3종, 발생 조건 모두 정확."),
    "Q04": ("1", "-", "CTXD_API_KEY는 정확. 그러나 우선순위를 '환경변수 먼저'로 오답. "
                      "실제 resolve_api_key는 인자 우선. CLI 전용 "
                      "_resolve_login_api_key(1등)를 SDK 함수로 혼동. "
                      "정답 청크가 2등에 있었음에도 유사 청크에 밀림."),
    "Q05": ("1", "A", "httpx를 말하지 못함. 정답 청크 miss로 imports 주입이 도달하지 못했다. "
                      "imports는 청크에 붙어 있어 그 청크가 검색되지 않으면 효력이 없다. "
                      "다만 무엇이 없어서 답할 수 없는지 정확히 지목."),
    "Q06": ("1", "-", "정답 청크 1등. CtxdError와 CtxdAuthError만 서술하고 "
                      "CtxdProtocolError 누락. 3등/4등에 있었으나 활용 못 함."),
    "Q07": ("1", "-", "in_index=N. 만점. 구조(Client가 AsyncClient를 감쌈)를 설명하고 "
                      "중복 방지라는 근거까지 코드에서 도출. 정답 청크는 miss였으나 "
                      "call_tool로 동일 결론에 도달. 컨트롤과 달리 실제 심볼명을 근거로 씀."),
    "Q08": ("1", "-", "in_index=N. 만점. CTXD_CREDENTIALS_PATH 전용 환경변수라는 "
                      "코드 근거를 찾아냈다. 컨트롤의 일반론과 질적으로 다름."),
    "Q09": ("1", "-", "in_index=N. 만점. AliasChoices를 짚고 text/snippet까지 확장. "
                      "추정임을 밝히고 근거 제시."),
    "Q10": ("1", "A", "정답 청크 miss(config.py::save_api_key). 흐름 3단계는 정확하나 "
                      "최종 저장 파일명(credentials.json)을 대지 못해 2점 기준 미달. "
                      "저장 함수 본문이 컨텍스트에 없다고 정직하게 밝힘."),
    "Q11": ("2", "-", "정답 청크 miss(Client._run)였으나 Client.search가 2등으로 들어와 "
                      "_run 호출이 본문에 보였다. 경로 전체 정확."),
    "Q12": ("2", "-", "정답 청크 1등. 우선순위 3단계 정확. "
                      "CLI 전용 함수와의 차이까지 구분해 설명."),
    "Q13": ("2", "-", "정답 청크 1등. 명령어 7개와 옵션까지 전부."),
    "Q14": ("0", "A", "/mcp 정규화를 말하지 못함. 정답 청크(_normalize_base_url) miss. "
                      "resolve_base_url만 보고 '변환 없음'이라 답했다. "
                      "in_index=Y인데 기권했으므로 0점. 검색 실패가 답변 실패로 "
                      "직결된 대표 사례."),
    "Q15": ("2", "-", "정답 청크 5등. del base_url, client_id를 짚어 정확히 '아니오'. "
                      "관례로 유추하면 반드시 틀리는 문항인데 코드 근거로 맞혔다. "
                      "검색 기여가 가장 뚜렷한 문항."),
}

SUMMARY = """## 집계

### 축 1 — 검색

| 지표 | 값 |
|---|---|
| Hit@1 | 5/15 (33.3%) |
| Hit@5 | 8/15 (53.3%) |
| MRR | 0.44 |

순위 분포: 1등 5개(Q02/Q06/Q09/Q12/Q13), 2등 1개(Q04), 5등 2개(Q03/Q15), miss 7개

### 축 2 — 답변 품질

| 유형 | 본 측정 | 컨트롤 | 차이 | 만점 |
|---|---|---|---|---|
| 개념 (Q01~Q03) | 5 | 0 | +5 | 6 |
| 식별자 (Q04~Q06) | 3 | 1 | +2 | 6 |
| 왜 (Q07~Q09) | 3 | 3 | 0 | 3 |
| 관계 (Q10~Q12) | 5 | 0 | +5 | 6 |
| 사실 (Q13~Q15) | 4 | 0 | +4 | 6 |
| **합계** | **20** | **4** | **+16** | **27** |

- 환각(H): 0건
- 기권(A): 3건 (Q05, Q10, Q14)

### 발견 1 — 검색 실패가 답변 실패로 직결되지 않는다

정답 청크를 놓친 7문항 중 Q01(2점)과 Q11(2점)은 만점을 받았다.
top_k=5가 완충 역할을 해, 정답 청크가 아니어도 인접 청크가 같은 결론에 이르게 했다.
answer() 독스트링의 "검색 1위가 정답이 아니어도 답변은 정확할 수 있다"가 외부 레포에서 실증되었다.

역으로 검색 성공이 답변 성공을 보장하지도 않는다.
Q04는 정답 청크가 2등에 있었으나, 1등으로 들어온 CLI 전용 함수
(_resolve_login_api_key)를 SDK 함수로 혼동해 우선순위를 틀렸다.
유사한 청크가 섞이면 정답 청크가 상위에 있어도 답이 오염된다.

### 발견 2 — 실패는 청크 경계에서 발생했다

Q02, Q05, Q14의 실패 원인이 모두 동일하다.
답이 AsyncClient.call_tool 본문에 있는데 그 청크가 top_k에 들지 못했다.
세 답변 모두 "call_tool 구현이 제공되지 않았다"고 스스로 지목했다.

이는 검색 알고리즘의 문제가 아니라 청킹 단위의 문제다.
call_tool은 HTTP 라이브러리, 요청 형식, 응답 파싱을 모두 담고 있어
여러 질문의 정답지 역할을 하는데, 청크 하나로 묶여 있어 함께 실패한다.

### 발견 3 — imports 주입은 청크가 검색되어야만 작동한다

Q05(HTTP 라이브러리)에서 httpx를 말하지 못했다.
Chunk.imports는 청크에 부착된 정보이므로, 그 청크가 검색되지 않으면 도달하지 못한다.
imports가 요약 품질을 높이는 것과, 질의 시점에 라이브러리명을 답하는 것은 별개의 문제다.
파일 수준(L1) 또는 레포 수준(L0) 요약에 의존성 정보를 올려야 해결된다.

### 발견 4 — "왜" 유형은 점수가 아니라 문장의 질에서 갈렸다

예상대로 점수 차이는 0이다(3 대 3). 그러나 답변의 성격이 완전히 다르다.

- 컨트롤: "openai, anthropic SDK가 이 패턴을 쓴다" — 일반론. 이 레포의 심볼 없음
- 본 측정: "Client.call_tool이 self._run(self._async_client.call_tool(...))을 호출한다" — 실제 코드 인용

Q08이 특히 명확하다. 컨트롤은 "보안상 분리하는 관행"이라는 일반론에 머물렀으나,
본 측정은 CTXD_CREDENTIALS_PATH라는 자격증명 전용 환경변수를 찾아내
경로를 독립 제어할 수 있다는 구조적 근거를 제시했다.

실험 B의 결론("버린 대안은 복원 불가")은 유지된다. 세 문항 모두 설계 의도 자체는
말하지 못했다. 다만 코드에 남은 구조로부터 도달 가능한 범위는 컨트롤보다 훨씬 넓었다.

### 발견 5 — 환각 0건

15문항 전부에서 없는 심볼을 지어내지 않았다.
답할 수 없을 때는 무엇이 없어서 답할 수 없는지를 지목했다(Q02, Q05, Q10, Q14).
프롬프트 개정(금지 규칙에 허용 규칙을 짝지음)이 과잉 유보로 기울지 않으면서
환각도 막았음을 확인했다.

### 한계

- 채점자가 1인이므로 채점자 간 일치도를 측정할 수 없다.
- __init__.py는 함수/클래스가 없어 청킹되지 않아 인덱스에 없다.
  패키지 공개 API 정보가 검색 대상에서 빠졌으며, Q01에 영향을 주었을 수 있다.
- 프롬프트를 개정한 뒤 측정했으므로 실험 B와 직접 비교할 수 없다.
- 문항 15개는 통계적 유의성을 논하기에 적은 표본이다.
"""


def fill(text: str) -> str:
    """빈 채점 칸을 순서대로 채운 문서를 돌려준다.

    Args:
        text (str): 원본 문서 전문.

    Returns:
        str: 채점이 채워진 문서.

    Raises:
        ValueError: 빈 채점 칸 수가 15개가 아닐 때.
    """
    blanks = EMPTY_BLOCK.findall(text)
    if len(blanks) != 15:
        raise ValueError(f"빈 채점 칸이 15개가 아닙니다: {len(blanks)}개")

    counter = {"i": 0}

    def replace(_match: re.Match) -> str:
        """등장 순서를 문항 번호로 삼아 채점 내용을 돌려준다."""
        counter["i"] += 1
        qid = f"Q{counter['i']:02d}"
        score, flag, memo = GRADES[qid]
        return f"- 점수(0~2): {score}\n- 플래그(H/A/-): {flag}\n- 메모: {memo}\n"

    return EMPTY_BLOCK.sub(replace, text)


def main() -> None:
    """파일을 읽어 채점을 채우고 집계를 덧붙인다."""
    text = TARGET.read_text(encoding="utf-8")

    if "## 집계" in text:
        print("이미 채점된 파일입니다. 중복 실행을 막습니다.")
        return

    filled = fill(text).rstrip() + "\n\n" + SUMMARY
    TARGET.write_text(filled, encoding="utf-8")
    print(f"채점 완료: {TARGET}")


if __name__ == "__main__":
    if not TARGET.exists():
        print(f"파일이 없습니다: {TARGET}")
        sys.exit(1)
    main()